class Solution(object):
    def arrayRankTransform(self, arr):
        sorting = sorted(set(arr))
        rankmap = {num: rank + 1 for rank, num in enumerate(sorting)}
        return [rankmap[num] for num in arr]
 
     



            
        

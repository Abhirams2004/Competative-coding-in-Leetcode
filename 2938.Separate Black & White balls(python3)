class Solution:
    def minimumSteps(self, s: str) -> int:
        n = len(s)
        res, cnt = 0, 0
        for i in range(n-1, -1, -1):
            if s[i] == '0':
                cnt += 1
            else:
                res += cnt
        return res
        
